{
  "name": "hiraya-app-starter",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun tsc --noEmit && bunx --bun vite build",
    "test": "bun test",
    "package": "bun run build && hiraya-app package dist"
  },
  "dependencies": {
    "@hiraya-team/apps-sdk": "2.3.0"
  },
  "devDependencies": {
    "@hiraya-team/app-cli": "2.3.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
